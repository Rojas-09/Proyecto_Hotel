[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Cobertura mínima requerida: 90%
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=80
    -v